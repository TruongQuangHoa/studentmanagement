@using StudentManagement.Models
@using PagedList.Core.Mvc
@model PagedList.Core.IPagedList<tblPost>

@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<main id="main" class="main">
    <div class="pagetitle">
        <h2>Danh sách bài đăng</h2>
        <section class="section dashboard">
            <div class="row">
                <div class="col-12">
                    <div class="card recent-sales overflow-auto">
                        <div class="card-body mt-4">
                            <table class="table table-bolderless datatable">
                                <thead>
                                    <tr>
                                        <th class="col-1 text-center">STT</th>
                                        <th class="col-5 text-center">Tiêu đề</th>
                                        <th class="col-1 text-center">Hiển thị</th>
                                        <th class="col-2 text-center">Chức năng</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @{
                                        int stt = 0;
                                    }
                                    @foreach (var item in Model)
                                    {
                                        stt++;
                                        <tr>
                                            <td class="text-center" scope="row">@stt</td>
                                            <td class="text-center text-primary" scope="row">@item.Title</td>
                                            <td class="text-center">
                                                <form asp-area="Admin" asp-controller="Post" asp-action="ToggleStatus"
                                                    method="post">
                                                    <input type="hidden" name="id" value="@item.PostID" />
                                                    <div class="form-check form-switch d-flex justify-content-center">
                                                        <input class="form-check-input toggle-status-switch" type="checkbox"
                                                            name="IsActive" @(item.IsActive ? "checked" : "")
                                                            onchange="this.form.submit()" />
                                                    </div>
                                                </form>
                                            </td>
                                            <td class="text-center" scope="row">
                                                <a asp-area="Admin" asp-controller="Post" asp-action="Edit"
                                                    asp-route-id="@item.PostID" class="btn btn-primary btn-sm"
                                                    title="Edit the post"><i class="bi bi-pencil"></i></a>
                                                <a asp-area="Admin" asp-controller="Post" asp-action="Delete"
                                                    asp-route-id="@item.PostID" class="btn btn-danger btn-sm"
                                                    title="Delete the post"><i class="bi bi-trash"></i></a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        </p>
    </div>
</main>