@using StudentManagement.Models;
@model IList<tblSubject>
@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<main id="main" class="main">
    <div class="pagetitle">
        <h2>Danh sách chức năng</h2>
        <p>
        <section class="section dashboard">
            <div class="row">
                <div class="col-12">
                    <div class="card recent-sales overflow-auto">
                        <div class="card-body mt-4">
                            <table class="table table-borderless datatable">
                                <thead>
                                    <tr>
                                        <th class="col-1 text-center">STT</th>
                                        <th class="col-1 text-center">Tên môn học</th>
                                        <th class="col-1 text-center">Số tiết</th>
                                        <th class="col-1 text-center">Học kỳ</th>
                                        <th class="col-1 text-center">Bộ môn</th>
                                        <th class="col-1 text-center">Hiển thị</th>
                                        <th class="col-1 text-center">Chức năng</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @{
                                        int stt = 0;
                                    }
                                    @foreach (var item in Model)
                                    {
                                        stt++;
                                        <tr>
                                            <td class="text-center">@stt</td>
                                            <td class="text-left text-primary text-center" scope="row">
                                                <a asp-area="Admin" asp-controller="Subject" asp-action="Details"
                                                    asp-route-id="@item.SubjectID">
                                                    @item.SubjectName
                                                </a>
                                            </td>
                                            <td class="text-center" scope="row">@item.NumberOfLesson</td>
                                            <td class="text-center" scope="row">@item.Semester</td>
                                            <td class="text-center" scope="row">@item.department?.DepartmentName</td>
                                            <td class="text-center">
                                                <form asp-area="Admin" asp-controller="Subject" asp-action="ToggleStatus"
                                                    method="post">
                                                    <input type="hidden" name="id" value="@item.SubjectID" />
                                                    <div class="form-check form-switch d-flex justify-content-center">
                                                        <input class="form-check-input toggle-status-switch" type="checkbox"
                                                            name="IsActive" @(item.IsActive ? "checked" : "")
                                                            onchange="this.form.submit()" />
                                                    </div>
                                                </form>
                                            </td>

                                            <td class="text-center" scope="row">
                                                <a asp-area="Admin" asp-controller="Subject" asp-action="Edit"
                                                    asp-route-id="@item.SubjectID" class="btn btn-primary btn-sm"
                                                    title="Edit the subject"><i class="bi bi-pencil"></i></a>
                                                <a asp-area="Admin" asp-controller="Subject" asp-action="Delete"
                                                    asp-route-id="@item.SubjectID" class="btn btn-danger btn-sm"
                                                    title="Delete the subject"><i class="bi bi-trash"></i></a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </section>
        </p>
    </div>
</main>